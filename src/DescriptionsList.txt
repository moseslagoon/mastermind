You GUI must contain the following parts:

A title bar with "Mastermind" as the label
A message area that appropriately displays one of the three messages
    A message indicating a game in progress and stating how many guesses remain
    A message indicating a game has been won
    A message indicating a game has been lost

A complete reprsentation of the game =

TILE PANE

    //not even using this lines, What inthe world is this used for? I don't even
    //konw
//    private TilePane makeButtonPane() {
//        TilePane tile = new TilePane();
//
//        tile.setPrefRows(4);
//        tile.setPrefColumns(3);
//
//        for (int j =1 ; j<=9; j++){
//            Button btn = new Button("" + j);
//            btn.setMaxSize(Double.MAX_VALUE, Double.MAX_VALUE);
//            tile.getChildren().add(btn);
//        }
//        return tile;
//    }



    /**
     * LEFT GRID PANE, WITH REGULAR GRID OF BUTTONS
     * @return
     */
    private GridPane leftGridPane() {
        GridPane grid = new GridPane();
        grid.setVgap(2);
        grid.setHgap(2);
        grid.setPadding(new Insets(20));

        int row;
        int col;
        for(row = 1; row<=20; row++) {
            for (col =1; col<=2; col++){
                Button btn = new Button();
                btn.setMinSize(20.0,20.00);
                grid.add(btn,col,row);
            }
        }
        // now add more buttons that have different sizes
        // or not
        grid.setGridLinesVisible(false);
        return grid;

    }
